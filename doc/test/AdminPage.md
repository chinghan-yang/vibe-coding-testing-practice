狀態：初始為 [ ]、完成為 [x]
注意：狀態只能在測試通過後由流程更新。
測試類型：前端元素、function 邏輯、Mock API、驗證權限...

---

## [x] 【前端元素】渲染管理後台基本元素
**範例輸入**：無
**期待輸出**：頁面上應正確顯示「管理後台」標題、返回連結、登出按鈕以及專屬功能的說明區塊

---

## [x] 【前端元素】根據角色顯示正確的標籤
**範例輸入**：`useAuth` 提供 `user.role === 'admin'` 的狀態
**期待輸出**：標籤應顯示「管理員」且帶有相應的 class

---

## [x] 【導航功能】點擊返回連結導向儀表板
**範例輸入**：點擊「← 返回」連結
**期待輸出**：應該導向至 `/dashboard` 路徑

---

## [x] 【function 邏輯】點擊登出按鈕
**範例輸入**：點擊「登出」按鈕
**期待輸出**：應呼叫 `logout()` 函數，並將路由取代導向至 `/login`，同時清除 state

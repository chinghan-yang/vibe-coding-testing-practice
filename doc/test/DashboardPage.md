狀態：初始為 [ ]、完成為 [x]
注意：狀態只能在測試通過後由流程更新。
測試類型：前端元素、function 邏輯、Mock API、驗證權限...

---

## [x] 【前端元素】渲染儀表板基本元素與用戶資訊
**範例輸入**：`useAuth` 提供包含 `username` 與 `role` 的使用者物件
**期待輸出**：頁面上應正確顯示使用者的頭像字母、歡迎詞（包含用戶名）以及角色標籤

---

## [x] 【驗證權限】管理員角色顯示管理後台連結
**範例輸入**：`useAuth` 提供 `user.role === 'admin'` 的狀態
**期待輸出**：畫面上應顯示「🛠️ 管理後台」的導航連結

---

## [x] 【驗證權限】一般用戶不顯示管理後台連結
**範例輸入**：`useAuth` 提供 `user.role === 'user'` 的狀態
**期待輸出**：畫面上不應顯示「🛠️ 管理後台」的導航連結

---

## [x] 【function 邏輯】點擊登出按鈕
**範例輸入**：點擊「登出」按鈕
**期待輸出**：應呼叫 `logout()` 函數，並將路由取代導向至 `/login`，同時清除 state

---

## [x] 【Mock API】商品列表載入中狀態
**範例輸入**：`productApi.getProducts()` 尚未回傳結果 (isLoading 為 true)
**期待輸出**：頁面上顯示「載入商品中...」的提示及載入動畫

---

## [x] 【Mock API】商品列表載入成功
**範例輸入**：`productApi.getProducts()` 成功回傳包含商品資料的陣列
**期待輸出**：頁面上正確渲染商品列表（每個商品包含圖示、名稱、描述、格式化的價格）

---

## [x] 【Mock API】商品列表載入失敗
**範例輸入**：`productApi.getProducts()` 回傳錯誤（例如 HTTP 500 或特定錯誤訊息）
**期待輸出**：頁面上顯示對應的錯誤訊息（例如「無法載入商品資料」或 API 帶回的訊息），並且隱藏載入中狀態及商品列表
